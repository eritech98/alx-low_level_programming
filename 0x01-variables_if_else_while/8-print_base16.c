#include <stdio.h>

/**
* main - Erick says start!
* Return: Always success 0
*/

int main(void)
{
	char g;

	char h;

	g = 'a';

	h = '1';

	while
		(h < 10) { putchar(h + '1');
		h++;
		}

	while

	(g <= 'f') { putchar(g);
		g++;
		}

	putchar('\n');

	return (0);

}

