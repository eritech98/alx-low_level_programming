#include <stdio.h>
#include "main.h"

/**
 * main - This function prints the name of the program
 * @argc: number of arguments(Argument count)
 * @argv: argument vector
 *
 * Return: will return an int which is 0
 */





int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);

	return (0);
}
