#include "main.h"
/**
 * _islower - A function checkikn lowercase character
 * @c: our character c
 * Return: data type which is an integer
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

